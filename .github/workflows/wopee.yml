name: Wopee Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# on:
#   workflow_run:
#     workflows: ["pages build and deployment"]
#     types:
#       - completed

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v3
        with:
          python-version: "3.x"
          architecture: "x64"

      - name: Get last package version
        run: |
          gh auth status
          # gh auth login --with-token < $GITHUB_TOKEN
          # gh auth status
          gh release download --repo autonomous-testing/bot-mvp --archive zip
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_30D_EXPIRATION }}
          # GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install wopee
        run: |
          pip install bot_mvp-0.1.2-py3-none-any.whl
          playwright install

      - name: Run wopee tests
        run: bot_mvp wopee/dronjo.cloud.yaml

      - name: Collect logs
        run: |
          mkdir logs
          mv bot.errors.log logs
          mv bot.log logs

      - uses: actions/upload-artifact@v2
        if: always()
        with:
          name: logs
          path: wopee/
          retention-days: 30
